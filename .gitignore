# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
venv/
.venv/

# Node / JS
node_modules/

# Build / cache
*.egg-info/
dist/
build/
.coverage

# Environment / secrets — NEVER commit
.env
.env.*
*.env
*secrets*
*credentials*
*.key
launch.env
auth-profiles.json

# Corpus / large files (downloaded, not committed)
enwik6
enwik8
enwik9
lab/data/enwik*
lab/data/*.zip
*.zip
benchmarks/results-cache/
benchmarks/results.csv

# OS
.DS_Store
.AppleDouble

# Editor
.idea/
.vscode/
*.swp
*~

# LocalSend full fork clone (only FORK-NOTES.md is tracked)
clients/localsend/*
!clients/localsend/FORK-NOTES.md

# Mesh runtime state (per-device)
mesh/room_state.json

# never commit private keys
mesh/.kin-4-janus-private-key.bin
mesh/.*-private-key.bin
mesh/kin-4-janus-genesis.txt
mesh/janus-sequence-2-envelope.json
